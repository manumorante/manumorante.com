const nextConfig = {
  // basePath: '/manumorante.com',
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['secure.gravatar.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig
